import 'package:flutter/material.dart';

class SkillsPanel extends StatelessWidget {
  const SkillsPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Árvore de Skills — em breve",
        style: TextStyle(color: Colors.white54),
      ),
    );
  }
}
